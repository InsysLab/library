package view;

import javafx.scene.layout.VBox;
import javafx.scene.text.Text;

public class AddPublication extends VBox {
	
	public AddPublication() {
		Text text = new Text("AddPublication");
		this.getChildren().add(text);
	}
	
}
